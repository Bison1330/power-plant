# R11 — runtime 224 dry run against the live dev chain

Runtime under test: `dev-ops/runtimes/vitreus_power_plant_testnet_runtime-224.compact.compressed.wasm`
built from `3c699c7` on `feature/solver-marketplace` (sha256 beside it:
`3854f9b18f12be1686e06602b96391bba10c6c0f091140053054f6ebed393483`; `…-224-try-runtime…` for the
dry run). Live chain: spec 223. Rollback: `…-223…`.

What 224 carries (`pallets/REVIEW_2026-09-17.md`): R11 — R9 (in 223) made a person's swap take its
input `Preserve` so a swap of one's whole VTRS balance keeps the ED instead of killing the account;
it applied that to whichever asset was the input, and pallet-assets reads `Preserve` as "keep the
minimum balance", so **on 223 nobody can sell their entire balance of a launch token on the pool**
(`Token(NotExpendable)`). `do_swap` now applies the caller's Preservation only when the input is
native; a token input is `Expendable`. Caught by the launchpad's own `fm03`/`fm13`, which were red on
223 and not run before it shipped; the fuzzer's allowlist accepted any funds error on a sell and is
narrowed. No storage change. Also in the tree since 223: `cargo +nightly fmt` over the pallets, the
runtime and the cli, and a clippy pass — no behaviour change in either.

## The run

`chopsticks try-runtime --checks All` against `ws://127.0.0.1:9945`:

```
Current runtime spec_version: 223 · New runtime spec_version: 224
🚚 VersionedMigrations for VitreusDex (0→1, 1→2, 2→3), Launchpad (0→1), LaunchTreasury (0→1),
   Privileges (0→1) "can be removed; on-chain is already at" their versions — all no-ops.
✅ Entire runtime state decodes without error. 83132 bytes total.
🩺 try-state checks ran for all 77 pallets; none failed.
```

## The storage diff (4 keys)

| key | change |
|---|---|
| `LaunchTreasury::VaultFunded` | `true` → `true` (the unversioned vault migration reruns; no-op) |
| `System::LastRuntimeUpgrade` | 224 |
| `:ethereum_schema`, `XcmPallet::CurrentMigration` | the permanent writes, as on every upgrade |

## After the real `setCode`

`system.lastRuntimeUpgrade` 224 (enacted 2026-09-17; a second `setCode` attempt the same day was
refused as a no-op, correctly). Observed on 224: Baltathar (`0x3Cd0…c6e0`) sold its entire
7,500,222.51-token position in launch 2's pool — `swap_exact_tokens_for_tokens(WithId(2^64+2),
Native, 7500222513950523396142390, 0)` — in block
`0xdb2133c342b7b1480b609e61d3fb229137b8008357ac6896601364e2f61dea2e`: `SwapExecuted`, token balance
0 afterwards, 992,445.57 → 992,562.70 VTRS. On 223 the same call was `Token(NotExpendable)`. The
site's swap MAX (frontend `473aa15`) offers the full token balance and, for VTRS, the balance less
the ED.
