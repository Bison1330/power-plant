# R8 — runtime 223 dry run against the live dev chain

Runtime under test: `dev-ops/runtimes/vitreus_power_plant_testnet_runtime-223.compact.compressed.wasm`
built from `9954ffa` on `feature/solver-marketplace` (sha256 beside it; `…-223-try-runtime…` for the
dry run). Live chain: spec 222, launch-treasury storage version 1. Rollback: `…-222…`.

What 223 carries: R8 only (`pallets/REVIEW_2026-09-17.md`) — `sellable` caps a sale at the vault's
reducible LNRG, because the broker sells with `keep_alive` and refuses to empty the asset account.
Found live on 222: the first compound after the R1 recount failed twice with `Token(NotExpendable)`
until an era's rounding left a wei behind (block 827,914). No storage change.

## The run

`chopsticks try-runtime --checks All` against `ws://127.0.0.1:9945`, one run:

```
Current runtime spec_version: 222 · New runtime spec_version: 223
🚚 Pallet "LaunchTreasury" VersionedMigration migration 0->1 can be removed; on-chain is already at StorageVersion(1).
✅ Entire runtime state decodes without error. 88078 bytes total.
🩺 try-state checks ran for all 77 pallets; none failed.
```

## The storage diff (4 keys)

| key | change |
|---|---|
| `LaunchTreasury::VaultFunded` | `true` → `true` (the unversioned vault migration reruns; no-op) |
| `System::LastRuntimeUpgrade` | 223 |
| `:ethereum_schema`, `XcmPallet::CurrentMigration` | the permanent writes, as on every upgrade |

## After the real `setCode`

`system.lastRuntimeUpgrade` 223. The next compound whose harvest attributes exactly the vault's
balance sells all but one wei instead of failing; `keeper_last_error` stays empty across eras.
