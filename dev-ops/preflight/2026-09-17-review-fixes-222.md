# Review fixes — runtime 222 dry run against the live dev chain

Runtime under test: `dev-ops/runtimes/vitreus_power_plant_testnet_runtime-222.compact.compressed.wasm`
built from `67cac50` on `feature/solver-marketplace` (`--features testnet-runtime`
for the upgrade artefact, sha256 in the `.sha256` beside it; `testnet-runtime,try-runtime`
for the dry run, `…-222-try-runtime.compact.compressed.wasm`). Live chain: spec 221,
launch-treasury storage version 0, one treasury (launch 5, TRSL). Rollback:
`…-221.compact.compressed.wasm`, read-only beside its `.sha256`.

What 222 carries (`pallets/REVIEW_2026-09-17.md`): the seven review fixes,
cherry-picked with `-x` from `design/launch-treasury` (R1 `df98ad5`, R2 `db4e717`,
R6 `8052365`, R7 `0b68065`, R3/R4/R5 `236bb14`), plus what only a running chain
needs: launch-treasury v0 → v1, the R1 recount of `LnrgAccounted`. Nothing else
changes a storage shape; the DEX and launchpad versions stay at 3 and 1.

## The run

`chopsticks try-runtime --checks All` against `ws://127.0.0.1:9945`
(`@acala-network/chopsticks@1.5.1`). Console:
`2026-09-17-review-fixes-222-chopsticks-try-runtime.log`; storage diff and
runtime logs: `…-try-runtime.json`. One run.

```
Current runtime spec_version: 221 · New runtime spec_version: 222
🚚 VitreusDex 0->1, 1->2, 2->3 and Launchpad 0->1 can be removed; on-chain already at 3 and 1
R1 pre_upgrade: LnrgAccounted 311124066036, vault LNRG 70958120325, 1 treasuries
🚚 Pallet "LaunchTreasury" VersionedMigration migrating storage version from 0 to 1.
R1 recount: LnrgAccounted 311124066036 -> 0 over 1 treasuries (vault holds 70958120325);
            70958120325 of stranded rewards become attributable at the next harvest
R1 post_upgrade: LnrgAccounted 0, claims 0, vault LNRG 70958120325
✅ Entire runtime state decodes without error. 84978 bytes total.
🩺 try-state checks ran for all 77 pallets, LaunchTreasury included; none failed.
```

## The storage diff, decoded (6 keys)

| key | change |
|---|---|
| `LaunchTreasury::LnrgAccounted` | 311,124,066,036 → 0: launch 5's claim is 0 (it sold everything it had been attributed), so the vault's 70,958,120,325 LNRG — thirteen eras of payouts the old rule stranded — go to TRSL at the next `harvest` |
| `LaunchTreasury::__STORAGE_VERSION__` | 0 → 1 |
| `LaunchTreasury::VaultFunded` | `true` → `true`: `FundLaunchTreasuryVault` is unversioned and runs again; it finds the vault funded (by the first fee, §9.6) and writes the flag it already had |
| `System::LastRuntimeUpgrade` | 222 |
| `:ethereum_schema`, `XcmPallet::CurrentMigration` | the permanent migrations' usual writes, as on every upgrade |

No account was touched; no balance moved.

## What to check after the real `setCode`

1. Node log: `R1 recount: LnrgAccounted 311124066036 -> 0` (the vault figure will be a few eras higher by then).
2. `system.lastRuntimeUpgrade` 222; storage versions launchpad 1, vitreusDex 3, launchTreasury 1.
3. The keeper's next poke: `harvest` attributes the whole vault balance to launch 5; `/treasury` reads it as LNRG accrued, then the next `compound` sells it — and after that sale `LnrgAccounted` should read 0 again, not the harvested figure (R1's fix on a live sale).
4. The site's shape check (`shapes: 38 assumptions hold on spec 222`) — no shape changed.
